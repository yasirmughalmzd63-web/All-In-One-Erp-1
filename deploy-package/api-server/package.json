{
  "name": "coins-sale-api-server",
  "version": "1.0.0",
  "description": "Coins Sale ERP — backend API (Express + PostgreSQL). Self-host on any Node.js 20+ host (Hostinger Node.js, VPS, etc.).",
  "private": true,
  "type": "module",
  "main": "dist/index.mjs",
  "scripts": {
    "start": "node --enable-source-maps ./dist/index.mjs"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
